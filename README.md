<!-- what about this method? explain how to do it.

create a 5th repo for the root folder with docker compose
git submodule at <repo url> <local file path of each Service>
<local file path of each Service> = . from the root folder (each service)

updating:
After any changes to service-contracts, update the GitHub submodule in your main project using:
git submodule update --remote --merge -->